import React from "react";
import { Text } from "react-native";
import { CompactResturantInfo } from "../../resturants/components/CompactResturantInfo";
export const MapCallout = ({ resturant = {} }) => {
  <CompactResturantInfo resturant={resturant} />;
};
